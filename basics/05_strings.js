//string interpolation using placeholder

const name="Atul"
const age=21

console.log(`my name is ${name} and age is ${age}`);

const myString="hello"

console.log(myString[0]);

/*
.length -> for length
.toUpperCase -> to convert to capital letters
.chatAt(index) -> check character at given index
.indexOf(character) -> get index of given character
.subString(start,end) -> to get a substring from given string
.slice(start,end) -> can use negative values
.trim() -> remove starting and end spaces from string
.replace(to_be_replaced, to_be_replaced_by) -> replace some characters in string
.includes(word) -> check whether word is present in string or not
.split(char) -> split the string in array where the char is obtained
*/